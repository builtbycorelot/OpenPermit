{
  "name": "openpermit-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prestart": "node scripts/inject-env.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^9.9.1",
    "vite": "^5.4.0"
  }
}
