{
  "name": "openpermit-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prestart": "node scripts/inject-env.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo \"(add eslint if desired)\" && exit 0",
    "test": "echo \"(add vitest if desired)\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
