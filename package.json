{
  "name": "openpermit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "test": "jest",
    "lint": "eslint src test",
    "serve": "http-server example"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "eslint": "^8.52.0",
    "prettier": "^3.1.0",
    "playwright": "^1.39.0",
    "http-server": "^14.1.1"
  }
}
