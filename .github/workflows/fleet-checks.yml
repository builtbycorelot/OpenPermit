name: rough-checks
on:
  push:
    branches: [main, master, production, "claude/**", "feat/**", "fix/**", "chore/**", "ci/**"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  rough-checks:
    uses: builtbycorelot/.github/.github/workflows/rough-checks.yml@main
