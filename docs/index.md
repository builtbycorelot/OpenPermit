# OpenPermit

This documentation site builds automatically on push to the default branch.

- Local preview: `make docs` → http://localhost:8000
- GitHub Pages URL (automatically computed by CI): `https://<owner>.github.io/<repo>/`
