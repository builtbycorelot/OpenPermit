services:
  app:
    build: .
    volumes:
      - .:/workspace
    ports:
      - "5173:5173"   # vite preview if present
      - "8000:8000"   # mkdocs serve
    environment:
      - OWNER
      - NAME
      - REPOSITORY
